# .github/workflows/generate-tests.yml
on:
  push:
    branches-ignore: [release/qa, main, develop]

jobs:
  generate-tests:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Run Claude Code with Vault context
        run: |
          CHANGED=$(git diff HEAD~1 --name-only)
          PROMPT=$(cat <<'EOF'
          Execute a test generation skill for the modified files.
          Save all test files inside the repository.
          Modified files:
          EOF
          )
          claude-smart --print "$PROMPT
          $CHANGED"
        
      - name: Commit generated tests
        run: |
          git config user.name "Claude Code Agent"
          git config user.email "claude@njplastic.com"
          git add src/test/ src/__tests__/
          git diff --staged --quiet || git commit -m "test: generated by Claude Code agent"
          git push
